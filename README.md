A Basic custom slider implemention. No library is used. Styled components have been used for styling.

clone and install dependencies to start.

to install dependencies
npm install

to start app
npm start